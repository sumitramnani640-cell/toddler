
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-edit mr-2"></i>Edit Customer: <%= customer.name %>
                </h3>
            </div>
            <form action="/admin/customers/<%= customer.id %>" method="POST">
                <input type="hidden" name="_method" value="PUT">
                <div class="card-body">
                    <div class="form-group">
                        <label for="name">Full Name <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="name" name="name" value="<%= customer.name %>" required>
                    </div>

                    <div class="form-group">
                        <label for="email">Email Address <span class="text-danger">*</span></label>
                        <input type="email" class="form-control" id="email" name="email" value="<%= customer.email %>" required>
                    </div>

                    <div class="form-group">
                        <label for="phone">Phone Number</label>
                        <input type="tel" class="form-control" id="phone" name="phone" value="<%= customer.phone || '' %>">
                        <small class="form-text text-muted">Optional phone number</small>
                    </div>

                    <div class="form-group">
                        <label for="status">Status</label>
                        <select class="form-control" id="status" name="status">
                            <option value="active" <%= customer.status === 'active' ? 'selected' : '' %>>Active</option>
                            <option value="inactive" <%= customer.status === 'inactive' ? 'selected' : '' %>>Inactive</option>
                        </select>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save mr-1"></i> Update Customer
                    </button>
                    <a href="/admin/customers" class="btn btn-secondary ml-2">
                        <i class="fas fa-times mr-1"></i> Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-info-circle mr-2"></i>Customer Information
                </h3>
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-4">Created:</dt>
                    <dd class="col-sm-8"><%= new Date(customer.createdAt).toLocaleDateString() %></dd>
                    
                    <dt class="col-sm-4">Updated:</dt>
                    <dd class="col-sm-8"><%= new Date(customer.updatedAt).toLocaleDateString() %></dd>
                    
                    <dt class="col-sm-4">ID:</dt>
                    <dd class="col-sm-8"><%= customer.id %></dd>
                </dl>
            </div>
        </div>

        <div class="card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-exclamation-triangle mr-2"></i>Important Notes
                </h3>
            </div>
            <div class="card-body">
                <div class="alert alert-warning">
                    <i class="fas fa-info-circle mr-2"></i>
                    <strong>Note:</strong> Changing a customer's email address will affect their login credentials.
                </div>
                <div class="alert alert-info">
                    <i class="fas fa-user-times mr-2"></i>
                    <strong>Deactivation:</strong> Setting status to inactive will prevent the customer from logging in.
                </div>
            </div>
        </div>
    </div>
</div>
